#DEFAULT

spring:
  datasource:
    url: "jdbc:oracle:thin:@connectiontest_high?TNS_ADMIN=/home/jhordycg/wallets"
    username: "ADMIN"
    password: "<password>"
    driver-class-name: "oracle.jdbc.OracleDriver"
  jpa:
    hibernate:
      ddl-auto: "create"
server:
  servlet:
    context-path: "/api"